# dsh-embedded-browser 的 bundle 补丁。
# GUI 内置插件同步（ensureBuiltinPlugins）后自动应用，向 profile 组合插入本插件。
# host 侧注册 webview_* 工具（经主进程浏览器桥驱动右侧 <webview>），
# client 侧注册右侧「浏览器」标签页（webview + 地址栏）。
- insert:
    - id: dsh-embedded-browser
      name: dsh-embedded-browser
      config:
        enabled: true
