# dsh-desktop-frame 的 bundle 补丁。
# Host 端无业务逻辑（桌面标题栏是纯 client 渲染），注册即让 dsh web 客户端加载本插件的 client 部分。
- insert:
    - id: dsh-desktop-frame
      name: dsh-desktop-frame
      config:
        enabled: true
