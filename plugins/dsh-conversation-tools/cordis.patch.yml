# dsh-conversation-tools 的 bundle 补丁。
# GUI 内置插件同步（ensureBuiltinPlugins）后自动应用，向 profile 组合插入本插件。
# 本插件只有服务端(Host webServer API)，无后台配置项，故仅有 enabled 开关。
- insert:
    - id: dsh-conversation-tools
      name: dsh-conversation-tools
      config:
        enabled: true