# dsh-browser-control 的 bundle 补丁。
# 服务端：读取 DSH_BROWSER_BRIDGE_URL（主进程浏览器桥端点）并加载 @deepseek-ai/dsh-mcp-client，
# 把内嵌浏览器能力注册为 mcp__browser__* 工具。地址缺失时仅告警、不注册（优雅降级）。
- insert:
    - id: dsh-browser-control
      name: dsh-browser-control
      config:
        enabled: true