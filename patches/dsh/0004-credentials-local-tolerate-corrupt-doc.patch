diff --git a/packages/credentials/credentials-local/src/index.ts b/packages/credentials/credentials-local/src/index.ts
index 9bdece7..7e44808 100644
--- a/packages/credentials/credentials-local/src/index.ts
+++ b/packages/credentials/credentials-local/src/index.ts
@@ -818,7 +818,21 @@ export class LocalCredentialProvider extends CredentialProvider {
       return
     }
     if (renderFlatLayoutMigration(text) !== undefined) text = await this.migrateFlatDocument()
-    const document = parseCredentialsDocument(text, this.spec.filename)
+    let document: CredentialsDocument
+    try {
+      document = parseCredentialsDocument(text, this.spec.filename)
+    } catch (error) {
+      // ???????????????????????? dsh ?????
+      // ??????????????????????????????????
+      // ????????????????????
+      this.ctx.logger.warn(
+        'credentials-local: ignoring unreadable %s at boot; no credentials loaded. Fix the document or remove it to start fresh.',
+        this.spec.filename,
+      )
+      this.ctx.logger.warn(error)
+      document = { refs: new Map<string, string>(), records: new Map<string, CredentialRecord>() }
+      text = `version: ${DOCUMENT_VERSION}\n`
+    }
     this.values = document.refs
     this.records = document.records
     this.text = text

